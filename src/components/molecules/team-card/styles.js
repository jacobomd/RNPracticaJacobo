import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        
    },
    image: {
        flex: 1,
        alignContent: "center",
        width: 400,
        height: 400,
    },
});

export default styles;