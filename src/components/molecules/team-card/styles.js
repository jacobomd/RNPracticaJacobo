import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        
    },
    image: {
        width: 200,
        height: 200,
        resizeMode: 'cover'
    },
});

export default styles;